# IoT-arduino-raspberryPI

@AGENTS.md

All project instructions live in AGENTS.md (single source of truth for every agent/tool).
Follow its 4-step workflow: plan doc → Arduino node → RPi gateway → full pipeline.
